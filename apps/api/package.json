{
  "name": "@secretnotebook/api",
  "version": "0.0.0",
  "private": true,
  "description": "Fastify moderation + relay service (Phase 1 stub)",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist .turbo *.tsbuildinfo",
    "lint": "eslint .",
    "typecheck": "tsc -b --noEmit",
    "test": "echo 'no tests yet' && exit 0"
  },
  "devDependencies": {
    "@secretnotebook/config-eslint": "workspace:*",
    "@secretnotebook/config-tsconfig": "workspace:*",
    "@types/node": "^20.14.0",
    "eslint": "^9.13.0",
    "typescript": "^5.6.3"
  }
}
