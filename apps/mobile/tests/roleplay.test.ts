import { describe, it } from '@jest/globals';

// Phase-1.5 R0: skipped. The role-play surface
// (start → rate → both-sides gratitude → +25 sweeper) is gone
// with the couple-loop cleanup. The new model is shared/secret
// notes with a library-drawn challenge mediating reveal;
// per-side acknowledgement coverage moves to R2 alongside the
// NoteUnlockAttempt state machine.
describe.skip('roleplay', () => {
  it('TODO restored in R2 via the note-unlock state machine', () => undefined);
});
