{
  "name": "@secretnotebook/mobile",
  "version": "0.0.0",
  "private": true,
  "description": "React Native client (Phase 1 stub — Expo bare workflow added in F3)",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist .turbo *.tsbuildinfo",
    "lint": "eslint .",
    "typecheck": "tsc -b --noEmit",
    "test": "echo 'no tests yet' && exit 0"
  },
  "devDependencies": {
    "@secretnotebook/config-eslint": "workspace:*",
    "@secretnotebook/config-tsconfig": "workspace:*",
    "eslint": "^9.13.0",
    "typescript": "^5.6.3"
  }
}
